# Add "attacked_flag" column to the measurements

# Apply the distribution-based attack and mark the attacked buses


# TODO: transform the final output into a graph
# Create a networkx graph (G) representation of the chosen power grid network
# G = pptop.create_nxgraph(net)
